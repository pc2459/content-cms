extends layout

block content
  .container.main
    if post
      .div.post-item
        h3 #{post.title}
        p.post-byline 
          | Written by 
          a(href="/users/#{post.owner}") #{post.ownerName}
        p.post-body #{post.body}
